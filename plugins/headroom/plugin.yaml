name: headroom
version: "0.1.0"
description: "Opt-in Phase 1 experiment for structured compression of selected large tool results. Disabled by default; only search_files and browser_snapshot are eligible. When content_router is enabled, uses headroom-ai ContentRouter + SmartCrusher for content-aware JSON/array compression."
author: "NousResearch"
hooks:
  - transform_tool_result
